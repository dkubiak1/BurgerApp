<div class="container">
<h1>Burgers!</h1>

<h2>Burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group radio">
    <label for="slee">Devoured?</label><br>
    <input type="radio" class="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" class="radio" name="devoured" value="0"> Appetizing
  </div>

  <button type="submit" class="btn btn-primary btn-block"><i class="fas fa-plus-circle"></i>
    &nbspAdd Burger
  </button>
</form>
</div>